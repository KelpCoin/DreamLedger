name: E0 Production Truth Probe

on:
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: dreamledger-e0-production-truth
  cancel-in-progress: false

env:
  BASE_URL: https://dreamledger.org

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout exact source
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 1

      - name: Define expected production SHA
        shell: bash
        run: echo "EXPECTED_SHA=$GITHUB_SHA" >> "$GITHUB_ENV"

      - name: Verify source SHA
        shell: bash
        run: |
          set -euo pipefail
          actual="$(git rev-parse HEAD)"
          echo "source_sha=$actual"
          test "$actual" = "$GITHUB_SHA"

      - name: Probe production routes
        shell: bash
        run: |
          set -euo pipefail
          probe() {
            local path="$1"
            local file="$2"
            local status
            status=$(curl -sS -o "$file" -w '%{http_code}' --max-time 20 --retry 3 --retry-delay 2 "$BASE_URL$path" || true)
            echo "$path http=$status"
            printf '%s\n' "$status" > "$file.status"
          }

          probe /healthz /tmp/healthz.json
          probe /version /tmp/version.json
          probe /board /tmp/board.html
          probe /api/molt-beach-inventory /tmp/inventory.json

      - name: Verify E0 contract
        shell: bash
        run: |
          set -euo pipefail
          health="$(cat /tmp/healthz.json.status)"
          version="$(cat /tmp/version.json.status)"
          board="$(cat /tmp/board.html.status)"
          inventory="$(cat /tmp/inventory.json.status)"

          echo "healthz_http=$health"
          echo "version_http=$version"
          echo "board_http=$board"
          echo "inventory_http=$inventory"

          test "$health" = "200"
          test "$version" = "200"
          test "$board" = "200"
          test "$inventory" = "200"

          python3 - <<'PY'
          import json
          expected = __import__('os').environ['EXPECTED_SHA']
          with open('/tmp/version.json', 'r', encoding='utf-8') as f:
              data = json.load(f)
          deployed = data.get('commit') or data.get('sha')
          print('deployed_sha=' + str(deployed))
          if deployed != expected:
              raise SystemExit(f'FAIL: production SHA {deployed!r} != workflow SHA {expected!r}')

          with open('/tmp/healthz.json', 'r', encoding='utf-8') as f:
              health = json.load(f)
          if health.get('status') != 'ok':
              raise SystemExit('FAIL: /healthz did not return {status: ok}')

          with open('/tmp/inventory.json', 'r', encoding='utf-8') as f:
              inventory = json.load(f)
          if not isinstance(inventory, (list, dict)):
              raise SystemExit('FAIL: inventory response was not JSON object/list')

          print('PASS: E0 contract is satisfied')
          PY

      - name: Emit E0 proof artifact
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p proof
          python3 - <<'PY'
          import json, os, time
          def status(path):
              try:
                  return open(path, encoding='utf-8').read().strip()
              except Exception:
                  return '0'
          statuses = {
              'healthz_http': status('/tmp/healthz.json.status'),
              'version_http': status('/tmp/version.json.status'),
              'board_http': status('/tmp/board.html.status'),
              'inventory_http': status('/tmp/inventory.json.status'),
          }
          proof = {
              'schema_version': 'BEC-E0-TRUTH-PROBE-2.0',
              'workflow': os.environ.get('GITHUB_WORKFLOW'),
              'run_id': os.environ.get('GITHUB_RUN_ID'),
              'source_sha': os.environ.get('GITHUB_SHA'),
              'expected_sha': os.environ.get('EXPECTED_SHA'),
              'base_url': os.environ.get('BASE_URL'),
              **statuses,
              'status': 'PRODUCTION_VERIFIED' if all(v == '200' for v in statuses.values()) else 'NOT_VERIFIED',
              'timestamp_utc': time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime())
          }
          with open('proof/e0-production-truth.json', 'w', encoding='utf-8') as f:
              json.dump(proof, f, indent=2)
          print(json.dumps(proof, indent=2))
          PY

      - name: Upload E0 proof
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: e0-production-truth-${{ github.sha }}
          path: proof/e0-production-truth.json
          if-no-files-found: error
          retention-days: 30
