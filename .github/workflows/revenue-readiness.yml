name: Revenue Readiness Gate

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'BEC-PRIME/catalog/**'
      - 'BEC-PRIME/scripts/Verify-RevenueReadiness.ps1'
      - 'BEC-PRIME/scripts/Verify-EconomicContract.ps1'
      - 'BEC-PRIME/package.json'
      - '.github/workflows/revenue-readiness.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: windows-latest
    steps:
      - name: Checkout exact triggering commit
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 1

      - name: Verify revenue readiness
        shell: powershell
        working-directory: BEC-PRIME
        run: |
          $ErrorActionPreference = 'Stop'
          powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\Verify-RevenueReadiness.ps1

      - name: Verify economic contract
        shell: powershell
        working-directory: BEC-PRIME
        run: |
          $ErrorActionPreference = 'Stop'
          powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\Verify-EconomicContract.ps1

      - name: Verify first-revenue activation remains payment-gated
        shell: powershell
        working-directory: BEC-PRIME
        run: |
          $ErrorActionPreference = 'Stop'
          $activation = Get-Content '.\scripts\Activate-FirstRevenue.ps1' -Raw
          if ($activation -notmatch 'AWAITING_FIRST_PAYMENT') { throw 'FAIL: activation gate no longer requires first-payment proof' }
          if ($activation -notmatch 'FIRST REVENUE ACTIVATION: PASS') { throw 'FAIL: activation proof marker missing' }
          Write-Host 'PASS: first-revenue activation remains payment-gated.'

      - name: Emit immutable proof metadata
        shell: powershell
        run: |
          $ErrorActionPreference = 'Stop'
          $proof = Get-Content 'BEC-PRIME\data\proofs\REVENUE-READINESS.json' -Raw | ConvertFrom-Json
          $proof | Add-Member -NotePropertyName github_sha -NotePropertyValue '${{ github.sha }}' -Force
          $proof | Add-Member -NotePropertyName run_id -NotePropertyValue '${{ github.run_id }}' -Force
          $proof | ConvertTo-Json -Depth 10 | Set-Content 'BEC-PRIME\data\proofs\REVENUE-READINESS.json' -Encoding utf8
          Get-Content 'BEC-PRIME\data\proofs\REVENUE-READINESS.json'

      - name: Upload revenue proof
        uses: actions/upload-artifact@v4
        with:
          name: revenue-readiness-proof
          path: |
            BEC-PRIME/data/proofs/REVENUE-READINESS.json
            BEC-PRIME/data/proofs/ECONOMIC-CONTRACT.json
          if-no-files-found: error
