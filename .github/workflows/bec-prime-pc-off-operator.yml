name: BEC Prime PC-Off Operator

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

permissions:
  contents: write

jobs:
  operate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: BEC-PRIME
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20 # No npm cache: BEC-PRIME has no package-lock.json.

      - name: Install
        run: npm ci

      - name: Compile IP
        run: npm run compile

      - name: Run Gauntlet
        run: npm run gauntlet

      - name: Run revenue autonomy cycle
        run: npm run autonomy:cycle

      - name: Run revenue ledger smoke test
        run: npm run smoke:revenue-ledger

      - name: Save operator proof
        if: always()
        run: |
          mkdir -p ../operator-proof
          printf '{\n  "workflow": "BEC Prime PC-Off Operator",\n  "pc_required": false,\n  "utc": "%s",\n  "compile": "executed",\n  "gauntlet": "executed",\n  "revenue_autonomy": "executed",\n  "revenue_ledger_smoke": "executed"\n}\n' "$(date -u +%Y-%m-%dT%H:%M:%SZ)" > ../operator-proof/AUTONOMOUS_OPERATOR_PROOF.json

      - name: Upload proof
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bec-prime-operator-proof
          path: BEC-PRIME/operator-proof/AUTONOMOUS_OPERATOR_PROOF.json
          if-no-files-found: warn
