name: Cortex CI/CD Live Release Gate

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      expected_sha:
        description: Commit SHA to verify in production
        required: false
        type: string

concurrency:
  group: cortex-cicd-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: CI - deterministic release gate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    outputs:
      release_sha: ${{ steps.release.outputs.sha }}
    steps:
      - name: Resolve release SHA
        id: release
        shell: bash
        env:
          INPUT_SHA: ${{ inputs.expected_sha }}
          EVENT_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          sha="${INPUT_SHA:-$EVENT_SHA}"
          test -n "$sha"
          echo "sha=$sha" >> "$GITHUB_OUTPUT"
          echo "RELEASE_SHA=$sha"
      - name: Checkout exact release SHA
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.release.outputs.sha }}
          fetch-depth: 1
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '24.14.1'
      - name: Install dependencies
        working-directory: BEC-PRIME
        shell: bash
        run: |
          set -euo pipefail
          if [ -f package-lock.json ]; then npm ci; else npm install; fi
      - name: Compile canonical release
        working-directory: BEC-PRIME
        run: npm run compile
      - name: Verify silo boundaries
        working-directory: BEC-PRIME
        run: npm run verify:silos
      - name: Verify commercial integration
        working-directory: BEC-PRIME
        run: npm run verify:integration
      - name: Verify worker pool
        working-directory: BEC-PRIME
        run: npm run worker:verify
      - name: Verify runtime and Stage 1 ledger proof
        working-directory: BEC-PRIME
        run: npm run runtime:verify
      - name: Verify Stage 1 proof contract
        working-directory: BEC-PRIME
        shell: bash
        run: |
          set -euo pipefail
          test -s RUN-PROOFS/STAGE-1-LOGIC-GATE.json
          node -e "const p=require('./RUN-PROOFS/STAGE-1-LOGIC-GATE.json'); if(p.status!=='PASS'||p.mode!=='SIMULATION'||p.lifecycle_complete!==true||p.revenue_claimed!==false||p.ra_000001_authorized!==false) process.exit(1); console.log('STAGE1_PROOF=PASS')"

      - name: Verify critical JavaScript syntax
        shell: bash
        run: |
          set -euo pipefail
          node --check BEC-PRIME/start.js
          node --check BEC-PRIME/runtime/ControlPlane.js
          node --check BEC-PRIME/routes/auth.js
          node --check BEC-PRIME/scripts/smoke-production.js
          node --check BEC-PRIME/scripts/smoke-front-door.js
      - name: Verify primary account pages
        shell: bash
        run: |
          set -euo pipefail
          for page in login.html register.html account.html; do
            test -f "BEC-PRIME/compiled/website/$page"
            grep -q '/api/account/' "BEC-PRIME/compiled/website/$page"
          done
          ! grep -q '/api/dreamiez/account/login' BEC-PRIME/compiled/website/login.html
      - name: Verify live commerce surfaces
        shell: bash
        run: |
          set -euo pipefail
          test -s public/automation-rescue.html
          grep -q 'LIVE OFFER' public/automation-rescue.html
          grep -q 'https://buy.stripe.com/' public/automation-rescue.html
          grep -q 'session_id' public/automation-rescue.html
          grep -q '/fulfilment.html?session_id=' public/automation-rescue.html
          grep -q 'NZ$99' public/automation-rescue.html
          test -s public/billboard.html
          grep -q 'Buy Founding Tile · NZ$50' public/billboard.html
          grep -q 'https://buy.stripe.com/' public/billboard.html
          test -s public/maximona.html
          grep -q 'Independent Production Verification' public/maximona.html
          grep -q 'NZ$1,500' public/maximona.html
          grep -q 'https://buy.stripe.com/dRmcN54zr29v2HZbN2dwc2C' public/maximona.html
          test -s public/offers.html
          echo 'LIVE_COMMERCE_SURFACES=PASS'
      - name: Write CI proof
        env:
          RELEASE_SHA: ${{ steps.release.outputs.sha }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p RUN-PROOFS
          python3 - <<'PY'
          import json, os, time
          with open('RUN-PROOFS/STAGE-1-LOGIC-GATE.json', encoding='utf-8') as f:
              stage1 = json.load(f)
          data = {
              'status': 'PASS', 'gate': 'CORTEX_CI', 'release_sha': os.environ['RELEASE_SHA'],
              'timestamp_utc': time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime()),
              'checks': ['compile','silo-boundaries','commercial-integration','worker-pool','runtime','stage-1-ledger-proof','syntax','primary-account-pages','live-commerce-surfaces'],
              'stage_1_status': stage1['status'], 'stage_1_mode': stage1['mode'],
              'ledger_head_hash': stage1['ledger_head_hash'], 'event_count': stage1['event_count'],
              'lifecycle_complete': stage1['lifecycle_complete'],
              'public_actions': 'approval-required', 'financial_actions': 'approval-required',
              'ra_000001_authorized': False, 'revenue_claimed': False
          }
          with open('RUN-PROOFS/CORTEX-CI-PROOF.json', 'w', encoding='utf-8') as f:
              json.dump(data, f, indent=2)
          print(json.dumps(data, indent=2))
          PY
      - name: Upload CI proof
        uses: actions/upload-artifact@v4
        with:
          name: cortex-ci-proof-${{ steps.release.outputs.sha }}
          path: |
            RUN-PROOFS/CORTEX-CI-PROOF.json
            RUN-PROOFS/STAGE-1-LOGIC-GATE.json
          if-no-files-found: error
          retention-days: 30

  production:
    name: CD - verify Render production release
    needs: ci
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      BASE_URL: https://dreamledger.org
      EXPECTED_SHA: ${{ needs.ci.outputs.release_sha }}
    steps:
      - name: Poll live version until exact SHA is deployed
        shell: bash
        run: |
          set -euo pipefail
          for i in $(seq 1 30); do
            body=$(curl -sS --max-time 10 "$BASE_URL/version" || true)
            live=$(python3 -c 'import json,sys; print(json.loads(sys.stdin.read()).get("commit", ""))' <<< "$body" 2>/dev/null || true)
            echo "poll=$i live_sha=$live expected_sha=$EXPECTED_SHA"
            if [ "$live" = "$EXPECTED_SHA" ]; then echo "LIVE_SHA_MATCH=PASS"; exit 0; fi
            sleep 20
          done
          echo "LIVE_SHA_MATCH=FAIL"
          exit 1
      - name: Verify required live surfaces
        shell: bash
        run: |
          set -euo pipefail
          check() { path="$1"; expected="$2"; code=$(curl -sS -o /tmp/response --retry 3 --retry-delay 2 --max-time 15 -w '%{http_code}' "$BASE_URL$path"); echo "$path HTTP=$code"; test "$code" = "$expected"; }
          check /healthz 200
          check / 200
          check /billboard 200
          check /maximona 200
          check /offers.html 200
          check /automation-rescue 200
          check /version 200
      - name: Verify live Maximona purchase surface
        shell: bash
        run: |
          set -euo pipefail
          body=$(curl -sS --fail-with-body --retry 3 --retry-delay 2 --max-time 20 "$BASE_URL/maximona")
          grep -q 'Independent Production Verification' <<< "$body"
          grep -q 'NZ$1,500' <<< "$body"
          grep -q 'https://buy.stripe.com/dRmcN54zr29v2HZbN2dwc2C' <<< "$body"
          echo 'LIVE_MAXIMONA=PASS'
      - name: Verify live Automation Rescue purchase surface
        shell: bash
        run: |
          set -euo pipefail
          body=$(curl -sS --fail-with-body --retry 3 --retry-delay 2 --max-time 20 "$BASE_URL/automation-rescue")
          grep -q 'LIVE OFFER' <<< "$body"
          grep -q 'https://buy.stripe.com/' <<< "$body"
          grep -q 'session_id' <<< "$body"
          grep -q '/fulfilment.html?session_id=' <<< "$body"
          grep -q 'NZ$99' <<< "$body"
          echo 'LIVE_AUTOMATION_RESCUE=PASS'
      - name: Verify live Billboard purchase surface
        shell: bash
        run: |
          set -euo pipefail
          body=$(curl -sS --fail-with-body --retry 3 --retry-delay 2 --max-time 20 "$BASE_URL/billboard")
          grep -q 'Buy Founding Tile · NZ$50' <<< "$body"
          grep -q 'https://buy.stripe.com/' <<< "$body"
          echo 'LIVE_BILLBOARD=PASS'
      - name: Write production proof
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p proof
          python3 - <<'PY'
          import json, os, time
          data={'status':'PASS','gate':'CORTEX_CD','release_sha':os.environ['EXPECTED_SHA'],'target':os.environ['BASE_URL'],'timestamp_utc':time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime()),'live_sha_verified':True,'surfaces_verified':['healthz','version','billboard','maximona','offers.html','automation-rescue'],'maximona_live':True,'purchase_executed':False,'revenue_claimed':False}
          with open('proof/cortex-production-proof.json','w',encoding='utf-8') as f: json.dump(data,f,indent=2)
          print(json.dumps(data,indent=2))
          PY
      - name: Upload production proof
        uses: actions/upload-artifact@v4
        with:
          name: cortex-production-proof-${{ env.EXPECTED_SHA }}
          path: proof/cortex-production-proof.json
          if-no-files-found: error
          retention-days: 30
