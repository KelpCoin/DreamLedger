name: Repair current CI batch
on:
  push:
    branches:
      - fix/canonical-billboard-checkout-verifier
permissions:
  contents: write
jobs:
  repair:
    if: github.event.head_commit.message != 'ci: apply current CI batch repair'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Repair known syntax regression
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          p = Path('BEC-PRIME/routes/billboard.js')
          s = p.read_text()
          old = "return send(res,400,{error:'Destination must be http(s)')} }const s=load(m),p=pos(s,z.w,z.h);"
          new = "return send(res,400,{error:'Destination must be http(s)'})}const s=load(m),p=pos(s,z.w,z.h);"
          if old not in s:
              raise SystemExit('expected billboard syntax regression not found')
          p.write_text(s.replace(old, new, 1))
          PY
      - name: Commit repair and remove one-shot workflow
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add BEC-PRIME/routes/billboard.js
          git rm .github/workflows/repair-ci-batch.yml
          git commit -m 'ci: apply current CI batch repair'
          git push
