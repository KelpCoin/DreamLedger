# Watchdog

Run deterministic checks first. Monitor started_at, heartbeat_at, produced_output_at, and completed_at independently. Emit typed anomalies. The monitored agent never supplies its own health verdict.
