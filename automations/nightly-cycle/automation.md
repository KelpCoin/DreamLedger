# Nightly Cycle

Observe first. Run at most one bounded operation per iteration. External action requires approval and a valid execution ticket. Every termination path produces a typed termination certificate. Append evidence before reporting completion.
