# CORTEX Builder v1 — Inspection (2026-09-15)

Canonical tip at inspect: `0bfc805b4e6d97677a2e6efefa87b9ac27ff11b5`

## What exists

| Piece | Location | Behaviour |
|-------|----------|-----------|
| Job envelope schema | `CORTEX/job_schema.json` | Envelope only; no lease columns |
| Local cortex worker | `CORTEX/cortex_worker.py` | Bridge claim smoke + manifest probe only |
| Proof writer | `CORTEX/proof_writer.py` | Hash + status; not SHA-bound to git |
| Worker pool | `BEC-PRIME/runtime/worker-pool.js` | File-based queue under `data/jobs`; kinds include `code_change`; **FORBIDDEN_EFFECTS**: money, checkout, public_post, production_mutation |
| Dispatch workflow | `.github/workflows/bec-worker-dispatch.yml` | **`permissions: contents: read`** — cannot push branch or open PR |
| Autonomous operator | `.github/workflows/bec-prime-autonomous-operator.yml` | Every 15m; compile + verify offers/silos; **`payment_verified: false`**, **`external_action_performed: false`**, **`pc_required: false`** |

## What was not found in-repo (do not invent)

- Table or module named `supervisor_jobs` with recoverable SQL schema
- Canonical lease column names (`lease_expires`, etc.) in this repository tree
- Any workflow with `contents: write` dedicated to Builder → PR for `cortex/job-*`

**Implication:** Lease recovery must be implemented only after the **live** job store (Supabase / agent-bridge / file queue) schema is read. Do not copy research SQL that assumes `status = 'CLAIMED'` only.

## Authority gap (why #304 cannot complete autonomously today)

1. Dispatch and autonomous operator are **read-only** on repository contents.
2. `cortex_worker.py` does not mutate the git tree.
3. Worker pool can *propose* `code_change` but the GitHub Actions boundary does not grant push/PR.
4. No SHA-bound proof that ties `proof.head_sha` to PR head.

## Builder v1 authority (locked)

**READ:** repository, issues, queue, CI, evidence, test output  
**WRITE:** isolated `cortex/job-*` branch, proof artifacts on that branch, PR to `main`  
**NEVER:** main, merge, production deploy, secrets, unrestricted workflow modification, external communication, financial action

## First proving job

**Issue #304** — PhinHaven rename (deterministic).  
Branch: **`cortex/job-304`**.  
Not PHINHAVEN-001.

## Acceptance for DONE

- Technical identifier set before == after (`kelplantis_*`, RPCs, migrations, historical proofs)
- User-facing Kelplantis / Finhaven → **PhinHaven** where in scope
- Tests relevant to touched surfaces pass
- `proof.head_sha == PR head SHA`
- PR targets `main`; worker never commits to `main`
- Database/job state, branch, SHA, proof, tests, PR all agree

Money river: untouched.
