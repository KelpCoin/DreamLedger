param($InputFile, $OutputFile); . "$PSScriptRoot\..\Config\DualLoop.Config.ps1"; if (!(Test-Path $InputFile)) { throw "Missing $InputFile" }; $input = Get-Content $InputFile -Raw; $prompt = "You are Agent 3 (Synthesize). Create a final run-sheet with sections: EXPERIMENT SNAPSHOT, CHECKLIST, ASSETS TO PREP, METRIC, SUCCESS LINE, IF IT WORKS, IF IT FLOPS. Make it executable by a tired human. Input: $input"; $res = Invoke-OllamaCompletion $prompt; Set-Content $OutputFile $res -Encoding UTF8
