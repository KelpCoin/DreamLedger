param($InputFile, $OutputFile); . "$PSScriptRoot\..\Config\DualLoop.Config.ps1"; if (!(Test-Path $InputFile)) { throw "Missing $InputFile" }; $input = Get-Content $InputFile -Raw; $prompt = "You are Agent 2 (Prune). Take the expanded brief and make it lean, sharp, and instantly runnable. Keep the same headings but tighten every section. Input: $input"; $res = Invoke-OllamaCompletion $prompt; Set-Content $OutputFile $res -Encoding UTF8
