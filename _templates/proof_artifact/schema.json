{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DreamLedger Proof Artifact",
  "type": "object",
  "required": ["classification"],
  "properties": {
    "classification": {
      "enum": ["CAPABILITY_PROOF", "TEST", "SIMULATED", "INTERNAL", "COMMERCIAL_EVIDENCE"],
      "description": "CAPABILITY_PROOF never promotes commercial hypotheses. Only COMMERCIAL_EVIDENCE does."
    }
  },
  "additionalProperties": true
}
