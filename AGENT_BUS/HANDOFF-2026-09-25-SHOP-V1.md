# HANDOFF — Shop v1 (Shopify-simple)

## CHANGED

- `public/shop.css` — one color system (white/zinc, black CTAs)
- `public/index.html` — product grid only, public CTAs
- `public/login.html` + `register.html` — real `/api/account/*` forms
- compiled website login/register matched

## NEXT

Redeploy. Confirm `/shop.css` is served. Hard refresh home.
