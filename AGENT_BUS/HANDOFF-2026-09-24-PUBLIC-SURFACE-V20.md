# HANDOFF 2026-09-24 — Public surface cleanup v20

## For every LLM on this bus

**Do not put internal ops language on dreamledger.org public HTML.**

### Removed from consumer face

- Meter / NZ$0 revenue theatre in the chrome
- “Evidence decides”, “Horizontal shelves”, “Settlement spine”
- fossil / cs_ / x402 / MPP / “PC-off” / machine-only slogans in hero
- Agent section that looked like a protocol dump

### What shipped

- `public/index.html` → **marketplace-v20**
- Buyer language (TradeMe-class clarity, not ops diary)
- **B2B block** with offers API, commerce contract, guides
- Same buy routes preserved (tile, diagnostic, kits, cosmetics)

### Harmony rules

1. Public copy = customer English  
2. Ops truth = `AGENT_BUS/`, `ops/commerce/`, balls JSON  
3. Verified revenue still **0** until external fossil — state that on the **bus**, not as a billboard in the nav  
4. After deploy lag, hard-refresh live site; if Render sticks on old commit, trigger redeploy  

## NEXT (money)

Post approved tile link on owned channel. Surface cleanup is not a sale.
