# HANDOFF — Supabase integration exploration

## Disk

`docs/SUPABASE-INTEGRATION.md` — map of roles, migrations themes, edge functions, client contract, agent limits.

## Limits

This agent still cannot query live Supabase. Operator verifies `phinhaven_client_builds` and migration apply state.
