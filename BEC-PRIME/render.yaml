services:
  - type: web
    name: dreamledger
    runtime: node
    buildCommand: powershell -NoProfile -Command "New-Item -ItemType Directory -Force -Path compiled\website | Out-Null; Copy-Item -Recurse -Force cubes\*\outputs\website\*\* compiled\website\"
    startCommand: node server.js
