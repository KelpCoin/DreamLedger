services:
  - type: web
    name: dreamledger
    runtime: node
    rootDir: BEC-PRIME
    buildCommand: test -f compiled/website/index.html && test -f compiled/website/mtg/index.html && echo "Verified committed compiled website"
    startCommand: node server.js
