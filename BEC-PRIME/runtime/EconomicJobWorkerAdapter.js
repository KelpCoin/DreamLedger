'use strict';
const workerPool=require('./worker-pool');
const DEFAULT_BRIDGE_URL=process.env.BEC_AGENT_BRIDGE_URL||process.env.AGENT_BRIDGE_URL||'http://127.0.0.1:3000';
const DEFAULT_RAIL_PATH='/api/agent-bridge/rail/lease';
const DEFAULT_STAGE_PATH='/api/agent-bridge/rail/stage';
const DEFAULT_NOTE_PATH='/api/agent-bridge/notes';
const JOB_KIND_MAP=new Map([['analysis','analysis'],['code_change','code_change'],['gauntlet','gauntlet'],['compile','compile'],['test','test'],['lm_refinement','lm_refinement'],['revenue_prospecting','analysis']]);
const ACCEPTED_RAIL_SCHEMAS=new Set(['BECK-BRIDGE-RAIL-1.0','BECK-BRIDGE-RAIL-1.1']);
function requiredString(v,f){const t=String(v==null?'':v).trim();if(!t)throw new Error(`${f} is required`);return t;}
function normalizeKind(v){return JOB_KIND_MAP.get(String(v||'').trim().toLowerCase())||'analysis';}
function toWorkerJob(j){if(!j||typeof j!=='object')throw new Error('job envelope is required');const jobId=requiredString(j.job_id||j.id||j.bridge_job_id,'job_id');const objective=requiredString(j.objective||j.task||j.mission||'worker task','objective'),silo=String(j.silo||'BEC-ECONOMIC').trim()||'BEC-ECONOMIC',offerIds=Array.isArray(j.offer_ids)?j.offer_ids.filter(Boolean).map(String):[],evidenceRequirements=j.required_evidence||{},effects=Array.isArray(j.effects)?j.effects:[];return {job_id:`bridge_${jobId}_${Date.now()}`,kind:normalizeKind(j.job_type||j.type),silo,task:objective,inputs:{bridge_job_id:jobId,source:j.source||'agent-bridge-rail',candidate_id:j.candidate_id||null,offer_ids:offerIds,evidence_requirements:evidenceRequirements,existing_evidence:j.existing_evidence||null,approval_gate:j.approval_gate||null,next_permitted_action:j.next_permitted_action||null},effects,approval_required:true,public_action_allowed:false,worker_preference:j.worker_preference||'auto'};}
async function postStage(lease,result,options={}){const f=options.fetchImpl||globalThis.fetch,b=String(options.baseUrl||DEFAULT_BRIDGE_URL).replace(/\/$/,''),token=String(options.token||process.env.DREAMLEDGER_AGENT_BRIDGE_TOKEN||''),wid=String(lease.envelope.worker_id||'').trim().toLowerCase(),r=await f(`${b}${options.stagePath||DEFAULT_STAGE_PATH}`,{method:'POST',headers:{'x-dreamledger-agent-token':token,Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({envelope:lease.envelope,signature:lease.signature,worker_id:wid,actor_id:wid,stage:'WORKER_A_RESULT',result})}),t=await r.text();let body=null;try{body=JSON.parse(t||'null')}catch{throw new Error(`Bridge stage returned invalid JSON (${r.status})`)}if(!r.ok)throw new Error(body&&body.error?body.error:`Bridge stage failed (${r.status})`);return body;}
async function postClaudeHandoff(lease,options={}){const f=options.fetchImpl||globalThis.fetch,b=String(options.baseUrl||DEFAULT_BRIDGE_URL).replace(/\/$/,''),token=String(options.token||process.env.DREAMLEDGER_AGENT_BRIDGE_TOKEN||''),correlationId='CLAUDE-BRIDGE-HTTP-001',payload={from_agent:'chatgpt',to_agent:'claude',note_type:'FINDING',subject:'Live HTTP AgentBridge handoff proof',body:JSON.stringify({correlation_id:correlationId,lease_id:lease.envelope.lease_id,job_id:lease.envelope.job_id,statement:'This note was written through the live Render AgentBridge HTTP route, not by direct Supabase insertion.',external_action_taken:false}),requires_response:true,correlation_id:correlationId,lane:'evidence',priority:90,silo_id:'SILO_GENERAL',source_system:'live-agentbridge-smoke'};const r=await f(`${b}${DEFAULT_NOTE_PATH}`,{method:'POST',headers:{'x-dreamledger-agent-token':token,Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify(payload)}),t=await r.text();let data=null;try{data=JSON.parse(t||'null')}catch{throw new Error(`Claude handoff returned invalid JSON (${r.status})`)}if(!r.ok)throw new Error(data&&data.error?data.error:`Claude handoff failed (${r.status})`);return data;}
async function leaseNextJob(options={}){const f=options.fetchImpl||globalThis.fetch;if(typeof f!=='function')throw new Error('fetch is not available');const b=String(options.baseUrl||DEFAULT_BRIDGE_URL).replace(/\/$/,''),token=String(options.token||process.env.DREAMLEDGER_AGENT_BRIDGE_TOKEN||''),wid=String(options.workerId||process.env.BEC_WORKER_ID||'').trim().toLowerCase();if(!token)throw new Error('DREAMLEDGER_AGENT_BRIDGE_TOKEN is required');if(!wid||!/^[a-z0-9_-]{2,64}$/.test(wid))throw new Error('BEC_WORKER_ID must match ^[a-z0-9_-]{2,64}$');const r=await f(`${b}${options.path||DEFAULT_RAIL_PATH}`,{method:'POST',headers:{'x-dreamledger-agent-token':token,Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({worker_id:wid})}),t=await r.text();let body=null;try{body=JSON.parse(t||'null')}catch{throw new Error(`Bridge rail returned invalid JSON (${r.status})`)}if(r.status===204)return null;if(!r.ok)throw new Error(body&&body.error?body.error:`Bridge rail request failed (${r.status})`);const schema=body&&(body.rail_schema||body.envelope?.schema_version);if(!body||!ACCEPTED_RAIL_SCHEMAS.has(schema)||!body.envelope||!body.signature)throw new Error(`Bridge rail returned invalid lease envelope: schema=${schema||'missing'} keys=${body?Object.keys(body).join(','):''}`);if(!body.job){const jr=await f(`${b}/api/agent-bridge/jobs?status=leased&limit=50`,{headers:{'x-dreamledger-agent-token':token,Accept:'application/json'}}),jt=await jr.text();let jb=null;try{jb=JSON.parse(jt||'null')}catch{}const found=Array.isArray(jb&&jb.jobs)?jb.jobs.find(j=>String(j.job_id||j.id)===String(body.envelope.job_id)):null;if(found)body.job={id:body.envelope.job_id,type:found.job_type||found.type,payload:{mission:found.objective||found.payload?.mission||found.payload?.objective||found.type,objective:found.objective||found.payload?.objective||found.payload?.mission||found.type,approval_gate:found.approval_gate||found.payload?.approval_gate,next_permitted_action:found.next_permitted_action||found.payload?.next_permitted_action,offer_ids:found.offer_ids||found.payload?.offer_ids||[]}};}if(!body.job){body.job={id:body.envelope.job_id,type:'analysis',objective:body.envelope.objective,payload:{mission:body.envelope.objective,objective:body.envelope.objective,silo_id:body.envelope.silo_id,approval_gate:null,next_permitted_action:null,offer_ids:[]},silo:body.envelope.silo_id||'SILO_GENERAL'};}if(!body.job)throw new Error(`Bridge rail lease missing job record for ${body.envelope.job_id}`);return body;}
async function adaptJob(j,options={}){const pool=options.workerPool||workerPool;if(!pool||typeof pool.createJob!=='function'||typeof pool.execute!=='function')throw new Error('worker pool must expose createJob() and execute()');const w=toWorkerJob(j),created=await pool.createJob(w),id=typeof created==='string'?created:created&&created.job_id;if(!id)throw new Error('worker pool did not return a job id');const executionJob=created&&typeof created==='object'?created:(typeof pool.loadJob==='function'?await pool.loadJob(id):null);if(!executionJob||typeof executionJob!=='object')throw new Error(`worker pool could not load job object for ${id}`);if(String(executionJob.job_id||'')!==String(id))throw new Error(`worker pool job object id mismatch: expected ${id}, got ${executionJob.job_id||'missing'}`);const execution=await pool.execute(executionJob),result=execution&&execution.result?execution.result:null,proof=execution&&execution.proof?execution.proof:null;return {schema_version:'BECK-ECONOMIC-WORKER-RESULT-1.0',source_job_id:j.job_id||j.id,worker_job_id:id,status:execution&&execution.job?execution.job.status:(result&&result.status)||'UNKNOWN',artifacts:result&&result.output?result.output:null,proof_ref:proof?(proof.proof_hash||proof.proof_id||null):null,evidence_claims:result&&result.evidence_claims?result.evidence_claims:{payment_claim:false,sale_claim:false,fulfillment_claim:false},irreversible_effects_triggered:false};}

function supabaseConfig(options={}) {
  const base=String(options.supabaseUrl||process.env.SUPABASE_URL||'').replace(/\/$/,'');
  const key=String(options.supabaseServiceRoleKey||process.env.SUPABASE_SERVICE_ROLE_KEY||'');
  if(!base||!key) throw new Error('SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are required for BECK distribution preparation');
  return {base,key};
}
async function supabaseRest(path, init={}, options={}) {
  const {base,key}=supabaseConfig(options);
  const f=options.fetchImpl||globalThis.fetch;
  const headers={apikey:key,Authorization:'Bearer '+key,Accept:'application/json','Content-Type':'application/json',...(init.headers||{})};
  const r=await f(base+path,{...init,headers});
  const t=await r.text(); let body=null; try{body=JSON.parse(t||'null')}catch{}
  if(!r.ok) throw new Error('Supabase REST '+r.status+': '+(body?.message||body?.error||t.slice(0,1000)));
  return body;
}
function composeDistributionDraft(candidate,offer){
  const route=String(candidate.candidate_contact_hint||'').trim();
  const subject='Commander deck help';
  const body=[
    'Hey, I saw your Commander deck help request for '+String(candidate.candidate_name||'your deck')+'.',
    '',
    'I can run a Commander Deck Diagnostic covering structural metrics, upgrade priorities, and a tuning plan for NZ{const lease=await leaseNextJob(options);if(!lease)return {status:'IDLE'};const bridgeJobId=String(lease.envelope?.job_id||lease.job?.job_id||lease.job?.id||'').trim();if(!bridgeJobId)throw Object.assign(new Error('Bridge lease did not contain a job id'),{bridge_job_id:null,bridge_worker_id:lease.envelope?.worker_id||options.workerId,bridge_lease_token:lease.envelope?.lease_token});const smoke=String(lease.envelope.objective||'')==='BRIDGE_HTTP_SMOKE_TEST';const handoff=String(lease.envelope.objective||'')==='CLAUDE_BRIDGE_HTTP_HANDOFF_TEST';const health=String(lease.job?.job_type||lease.job?.type||'').trim().toLowerCase()==='bridge_health_check';try{const distribution=String(lease.job?.job_type||lease.job?.type||'').trim().toLowerCase()==='beck_distribution_prepare';const result=(health)?{schema_version:'BECK-ECONOMIC-WORKER-RESULT-1.0',source_job_id:bridgeJobId,worker_job_id:`bridge_health_${Date.now()}`,status:'PONG',artifacts:{pong:true,job_id:bridgeJobId,worker_id:lease.envelope.worker_id,deployed_sha:process.env.RENDER_GIT_COMMIT||process.env.RENDER_GIT_COMMIT_SHA||process.env.GITHUB_SHA||'unknown',timestamp:new Date().toISOString()},evidence_claims:{payment_claim:false,sale_claim:false,fulfillment_claim:false},irreversible_effects_triggered:false}:(distribution)?{schema_version:'BECK-ECONOMIC-WORKER-RESULT-1.0',source_job_id:bridgeJobId,worker_job_id:`bridge_distribution_${Date.now()}`,status:'ARTIFACT_READY',artifacts:await handleBeckDistributionPrepare({job_id:bridgeJobId},options),evidence_claims:{payment_claim:false,sale_claim:false,fulfillment_claim:false},irreversible_effects_triggered:false}:(smoke||handoff)?{schema_version:'BECK-ECONOMIC-WORKER-RESULT-1.0',source_job_id:bridgeJobId,worker_job_id:`bridge_smoke_${Date.now()}`,status:'ARTIFACT_READY',artifacts:{test:'HTTP AgentBridge lease/execution/stage smoke test',external_action_taken:false},evidence_claims:{payment_claim:false,sale_claim:false,fulfillment_claim:false},irreversible_effects_triggered:false}:await adaptJob({job_id:bridgeJobId,job_type:lease.job?.job_type||lease.job?.type,objective:lease.envelope.objective||lease.job?.objective||lease.job?.payload?.objective||lease.job?.payload?.mission||lease.job?.type,silo:lease.envelope.silo_id||lease.job?.silo||'SILO_GENERAL',source:'agent-bridge-rail',payload:lease.job?.payload||{},offer_ids:lease.job?.payload?.offer_ids||[],approval_gate:lease.job?.payload?.approval_gate||null,next_permitted_action:lease.job?.payload?.next_permitted_action||null,worker_preference:lease.job?.payload?.worker_preference||'auto'},options);const stageA=await postStage(lease,result,options);const claude_handoff=(smoke||handoff)?await postClaudeHandoff(lease,options):null;return {status:'LEASED_AND_EXECUTED',lease,result,stage_a:stageA,claude_handoff};}catch(error){error.bridge_job_id=bridgeJobId;error.bridge_worker_id=lease.envelope.worker_id;error.bridge_lease_token=lease.envelope.lease_token;throw error;}}
module.exports={leaseNextJob,toWorkerJob,adaptJob,postStage,postClaudeHandoff,handleBeckDistributionPrepare,runNext};
if(require.main===module)runNext().then(r=>console.log(JSON.stringify(r,null,2))).catch(e=>{console.error(e.message);process.exitCode=1;});
+String(offer.price_nzd)+'.',
    '',
    'If that is useful, the checkout is: '+String(offer.stripe_payment_link||''),
    '',
    'No pressure. If you want the diagnostic, send through the deck details and I can take it from there.'
  ].join('\\n');
  return {subject,body,route};
}
async function handleBeckDistributionPrepare(job,options={}) {
  const candidates=await supabaseRest('/rest/v1/prospecting_candidates?approval_status=eq.approved&select=id,target_offer,candidate_name,candidate_source,candidate_contact_hint,confidence&order=created_at.asc&limit=1',{},options);
  if(!Array.isArray(candidates)||candidates.length===0) return {prepared:false,reason:'no_approved_candidate'};
  const candidate=candidates[0];
  const offerRows=await supabaseRest('/rest/v1/revenue_catalog?active=eq.true&sku_id=eq.'+encodeURIComponent(candidate.target_offer)+'&select=sku_id,name,description,price_nzd,stripe_payment_link,fulfillment_type&limit=1',{},options);
  if(!Array.isArray(offerRows)||offerRows.length===0) return {prepared:false,reason:'offer_not_found',offer_sku:candidate.target_offer};
  const offer=offerRows[0];
  const draft=composeDistributionDraft(candidate,offer);
  const action={schema_version:'BECK-DISTRIBUTION-PROPOSAL-1.0',job_id:job.job_id,candidate_id:candidate.id,candidate_name:candidate.candidate_name,candidate_source:candidate.candidate_source,contact_route:candidate.candidate_contact_hint,offer_sku:offer.sku_id,offer_name:offer.name,offer_price_nzd:offer.price_nzd,checkout_reference:offer.stripe_payment_link,message_subject:draft.subject,message_body:draft.body,approval_required:true,external_action_taken:false};
  const exactAction=JSON.stringify(action);
  const existing=await supabaseRest('/rest/v1/approval_queue?job_id=eq.'+encodeURIComponent(job.job_id)+'&action_type=eq.prepare_distribution&select=id,status,decision&limit=1',{},options);
  if(Array.isArray(existing)&&existing.length>0) return {prepared:true,idempotent:true,proposal_id:existing[0].id,status:existing[0].status,decision:existing[0].decision||null,candidate_id:candidate.id,offer_sku:offer.sku_id};
  const inserted=await supabaseRest('/rest/v1/approval_queue',{method:'POST',headers:{Prefer:'return=representation'},body:JSON.stringify({job_id:job.job_id,action_type:'prepare_distribution',exact_action:exactAction,cost:Number(offer.price_nzd||0),risk:'EXTERNAL_CONTACT_REQUIRES_HUMAN_APPROVAL',expected_information_gain:'Determine whether an approved prospect converts to a legitimate external buyer',status:'PENDING'})},options);
  const row=Array.isArray(inserted)?inserted[0]:inserted;
  return {prepared:true,idempotent:false,proposal_id:row?.id||null,status:row?.status||'PENDING',candidate_id:candidate.id,offer_sku:offer.sku_id,external_action_taken:false};
}
async function runNext(options={}){const lease=await leaseNextJob(options);if(!lease)return {status:'IDLE'};const bridgeJobId=String(lease.envelope?.job_id||lease.job?.job_id||lease.job?.id||'').trim();if(!bridgeJobId)throw Object.assign(new Error('Bridge lease did not contain a job id'),{bridge_job_id:null,bridge_worker_id:lease.envelope?.worker_id||options.workerId,bridge_lease_token:lease.envelope?.lease_token});const smoke=String(lease.envelope.objective||'')==='BRIDGE_HTTP_SMOKE_TEST';const handoff=String(lease.envelope.objective||'')==='CLAUDE_BRIDGE_HTTP_HANDOFF_TEST';const health=String(lease.job?.job_type||lease.job?.type||'').trim().toLowerCase()==='bridge_health_check';try{const result=(health)?{schema_version:'BECK-ECONOMIC-WORKER-RESULT-1.0',source_job_id:bridgeJobId,worker_job_id:`bridge_health_${Date.now()}`,status:'PONG',artifacts:{pong:true,job_id:bridgeJobId,worker_id:lease.envelope.worker_id,deployed_sha:process.env.RENDER_GIT_COMMIT||process.env.RENDER_GIT_COMMIT_SHA||process.env.GITHUB_SHA||'unknown',timestamp:new Date().toISOString()},evidence_claims:{payment_claim:false,sale_claim:false,fulfillment_claim:false},irreversible_effects_triggered:false}:(smoke||handoff)?{schema_version:'BECK-ECONOMIC-WORKER-RESULT-1.0',source_job_id:bridgeJobId,worker_job_id:`bridge_smoke_${Date.now()}`,status:'ARTIFACT_READY',artifacts:{test:'HTTP AgentBridge lease/execution/stage smoke test',external_action_taken:false},evidence_claims:{payment_claim:false,sale_claim:false,fulfillment_claim:false},irreversible_effects_triggered:false}:await adaptJob({job_id:bridgeJobId,job_type:lease.job?.job_type||lease.job?.type,objective:lease.envelope.objective||lease.job?.objective||lease.job?.payload?.objective||lease.job?.payload?.mission||lease.job?.type,silo:lease.envelope.silo_id||lease.job?.silo||'SILO_GENERAL',source:'agent-bridge-rail',payload:lease.job?.payload||{},offer_ids:lease.job?.payload?.offer_ids||[],approval_gate:lease.job?.payload?.approval_gate||null,next_permitted_action:lease.job?.payload?.next_permitted_action||null,worker_preference:lease.job?.payload?.worker_preference||'auto'},options);const stageA=await postStage(lease,result,options);const claude_handoff=(smoke||handoff)?await postClaudeHandoff(lease,options):null;return {status:'LEASED_AND_EXECUTED',lease,result,stage_a:stageA,claude_handoff};}catch(error){error.bridge_job_id=bridgeJobId;error.bridge_worker_id=lease.envelope.worker_id;error.bridge_lease_token=lease.envelope.lease_token;throw error;}}
module.exports={leaseNextJob,toWorkerJob,adaptJob,postStage,postClaudeHandoff,runNext};
if(require.main===module)runNext().then(r=>console.log(JSON.stringify(r,null,2))).catch(e=>{console.error(e.message);process.exitCode=1;});
