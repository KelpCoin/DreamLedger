services:
  - type: web
    name: dreamledger-frontend
    runtime: static
    buildCommand: echo "Static site built"
    staticPublishPath: ./deploy
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
