services:
  - type: web
    name: dreamledger
    runtime: node
    rootDir: BEC-PRIME
    buildCommand: npm install && node --check server.js && test -f compiled/website/index.html && test -f compiled/website/mtg/index.html && test -f compiled/website/commander/index.html && test -f compiled/website/pokemon/index.html && test -f compiled/website/vinyl/index.html && test -f compiled/website/videogames/index.html && test -f compiled/website/retro/index.html && test -f catalog/products/MTG-URZAS-LEGACY-PALINCHRON-FOIL-001.json && test -f catalog/products/COMMANDER-DECK-DIAGNOSTIC-001.json && echo "Verified DreamLedger production assets"
    startCommand: node server.js
    healthCheckPath: /healthz
    disk:
      name: dreamledger-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: PUBLIC_BASE_URL
        value: https://dreamledger.org
      - key: LEDGER_DATA_DIR
        value: /var/data/ledger
      - key: PROOF_DATA_DIR
        value: /var/data/proofs
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
