DreamLedger production deployment trigger.

Purpose: force a fresh Render deployment from main so production can be verified against the Git commit SHA.

Release gate:
- GET /login must resolve to the canonical primary DreamLedger login surface.
- GET /login.html must return HTTP 200.
- POST /api/account/register must create a primary account.
- POST /api/account/login must authenticate the primary account.
- GET /api/account/me must retain the authenticated session.
- Dreamiez must remain optional and must not be the primary authentication authority.

Do not remove until the production /version and account smoke checks have passed.

2026-08-21 CI/CD execution marker: trigger the evidence-gated Render deployment from current main and verify live commit provenance before release.
